import { CorganizeFile } from "typedefs/CorganizeFile";
import { SortOrder } from "./types";

export const createMegaComparer = (sortOrder: SortOrder[]) => (a: CorganizeFile, b: CorganizeFile): number => {
    // TODO
}